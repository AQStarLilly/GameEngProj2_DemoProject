.title {
}

.interaction-popup {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: ;
    color: white;
    font-size: 22px;
    unity-text-align: center;
    background-color: rgba(0, 0, 0, 0.5);
    padding: 8px 16px;
    border-radius: 8px;
    opacity: 0;
    transition: opacity 0.3s ease-in-out;
}
